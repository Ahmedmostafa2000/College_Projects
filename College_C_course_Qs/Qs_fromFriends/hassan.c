#include <stdio.h>
void main(){
	int lines,i,counter=0;

	scanf("%d",&lines);
	int numbers[lines];
    for (i = 0; i < lines; i++){
        scanf("%s",&numbers[i]);
    for(i = 0,i<lines,i++){
        printf(numbers[i])
        }
    }
	}


